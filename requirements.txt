# Python Task Manager Dependencies
# ระบบจัดการงานประจำวันด้วย Python

# Python Standard Library (ไม่ต้องติดตั้งเพิ่ม)
# - json: สำหรับการจัดการข้อมูล JSON
# - os: สำหรับการจัดการไฟล์และโฟลเดอร์
# - datetime: สำหรับการจัดการวันที่และเวลา
# - typing: สำหรับ type hints

# ไม่มี external dependencies ที่จำเป็น
# โปรแกรมใช้เฉพาะ Python Standard Library

# สำหรับการพัฒนา (ไม่บังคับ)
# pytest>=6.0.0          # สำหรับการทดสอบ
# black>=21.0.0           # สำหรับการจัดรูปแบบโค้ด
# flake8>=3.8.0           # สำหรับการตรวจสอบโค้ด
